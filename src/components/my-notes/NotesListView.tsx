import { TNote } from '@/app/(authenticated)/my-notes/page';
import React from 'react';

const NotesListView = ({ data }: { data: TNote[] }) => {
    return <div>NotesListView</div>;
};

export default NotesListView;
